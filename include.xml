<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<ndll name="mailsender" if="ios" />
	
	<!-- Use the following for an Android Java extension, not needed otherwise -->
	<section if="android">
		<dependency name="mailsender" path="dependencies/android" />
		<android extension="org.haxe.extension.MailSender" />
	</section>

</project>